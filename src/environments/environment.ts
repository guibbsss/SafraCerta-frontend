export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
  /** Alinhado a app.registro.perfil-id no backend — perfil temporário "Solicitado" a ocultar na aprovação */
  registroPerfilId: 2
};
